<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <title> <%= title %> </title>
</head>
<body>
    <div class="container">
        <h1> <%= title %> </h1>
        <form action="/" method="post" class="mb-2">
            <div class="form-group mb-2">
                <input type="text" name="title" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Create</button>
        </form>
        <div class="card">
            <ul class="list-group">
                <% for (let i=0; i < notes.length; i++) {%>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span id="sp-<%= notes[i].id %>"><%= notes[i].title %></span>
                    <div class="d-flex justify-content-end">
                    <button class="btn btn-primary me-2" data-type="edit" data-id="edit-<%= notes[i].id %>">Edit</button>
                    <button class="btn btn-danger" data-type="remove" data-id="remove-<%= notes[i].id %>">&times;</button></div>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
<script src="/app.js"></script>
</body>
</html>